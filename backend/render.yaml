services:
  - type: web
    name: estrapol-backend
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGODB_URI  # La variable se configura MANUALMENTE en Render
        syncEnv: true
      - key: JWT_SECRET
        syncEnv: true